<UserControl x:Class="Argenta.Wpf.Views.Catalogos.TiposCambioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="750"
             Foreground="{DynamicResource TextoPrimarioBrush}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Encabezado: título + búsqueda + agregar -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Tipo de Cambio" Style="{StaticResource TituloSeccionStyle}" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Style="{StaticResource CampoBusquedaStyle}" VerticalAlignment="Center" Margin="0,0,12,0"
                     Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="2" Content="+ Nuevo" Style="{StaticResource BotonAgregarStyle}"
                    Command="{Binding AgregarCommand}"/>
        </Grid>

        <Button Grid.Row="1" Content="Importar archivo del Banguat..." Command="{Binding ImportarArchivoCommand}"
                Style="{StaticResource BotonSecundarioStyle}" HorizontalAlignment="Left" Margin="0,0,0,16"/>

        <!-- Tabla -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding TiposCambio}" Style="{StaticResource EstiloDataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="1*"/>
                <DataGridTextColumn Header="Valor (Q por USD)" Binding="{Binding Valor, StringFormat=N5}" Width="1*"/>
                <DataGridTemplateColumn Header="Acción" Width="1.2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Editar" Style="{StaticResource BotonAccionTablaStyle}"
                                        Command="{Binding DataContext.EditarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="Eliminar" Style="{StaticResource BotonAccionEliminarStyle}"
                                        Command="{Binding DataContext.EliminarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding Mensaje}" Margin="0,12,0,0" Foreground="{DynamicResource ExitoBrush}" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
