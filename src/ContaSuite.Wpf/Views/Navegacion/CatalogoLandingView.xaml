<UserControl x:Class="ContaSuite.Wpf.Views.Navegacion.CatalogoLandingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900"
             Foreground="{DynamicResource TextoPrimarioBrush}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Titulo}" Style="{StaticResource TituloSeccionStyle}" Margin="0,0,0,20"/>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Tarjetas}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource TarjetaStyle}" Width="220" Height="120" Margin="0,0,20,20">
                        <Button Command="{Binding Comando}" Background="Transparent" BorderThickness="0" Cursor="Hand"
                                HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding Nombre}" FontSize="17" FontWeight="Bold"
                                           Foreground="{DynamicResource AcentoBrush}" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Descripcion}" Style="{StaticResource SubtituloStyle}"
                                           Margin="0,8,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Button>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
